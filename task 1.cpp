//wap to create record of books of A library having fields book name ,author, name ,publisher ,name , edition abd price 
//then print record  of all books in the asscending order of their prices.
#include<stdio.h>
main()

struct student 
{
struct student a[5],c;
int  i;
printf("enter details of all student ");
for (i=0;i<5;i++)
{
	scanf("%d%s%f",&a[i].rollno,a[i].name,&a[i].per);
	}	
	for(p=1;p<)
}
